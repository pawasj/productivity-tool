import JoinForm from "./JoinForm";

export const metadata = {
  title: "Join BCC Media Network — Creator & Page Registration",
  description: "Register your creator profile or community page with BCC Media Network to get featured in brand campaigns.",
};

export default function JoinPage() {
  return <JoinForm />;
}
